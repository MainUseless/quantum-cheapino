name: Build RMK firmware
on:
  workflow_dispatch:
  push:
    paths:
      - "**"

jobs:
  build-firmware:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Rust Toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          targets: riscv32imc-unknown-none-elf

      - name: Cache Cargo Binaries & Registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            rmk/target/
          key: ${{ runner.os }}-cargo-build-${{ hashFiles('**/keyboard.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-build-

      - name: Install Tools if Missing
        run: |
          # Only install if they aren't already restored from the cache
          if ! command -v rmkit &> /dev/null; then
            cargo install rmkit --force
          fi
          if ! command -v espflash &> /dev/null; then
            cargo install espflash --force
          fi

      - name: Setup Project Workspace
        run: |
          rmkit create --keyboard-toml-path keyboard.toml --vial-json-path vial.json --target-dir rmk

      - name: Inject Custom Bidirectional Matrix
        run: |
          cp src/main.rs rmk/src/main.rs
          # Add vial_lock feature for Vial matrix tester (pulls in host_security)
          python3 -c "
          import re
          with open('rmk/Cargo.toml') as f: t = f.read()
          if 'vial_lock' not in t:
              t = re.sub(r'(features\s*=\s*\[)', r'\1\"vial_lock\", ', t, count=1)
          with open('rmk/Cargo.toml', 'w') as f: f.write(t)
          "
          cat rmk/Cargo.toml

      - name: Compile Custom Firmware
        env:
          RUST_MIN_STACK: 16777216
        run: |
          cd rmk
          cargo +nightly build --release --target riscv32imc-unknown-none-elf

      - name: Convert to Flashable Binary
        run: |
          cd rmk
          espflash save-image --chip esp32c3 --merge "target/riscv32imc-unknown-none-elf/release/cheapino_w" firmware.bin

      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: cheapino-firmware
          path: rmk/firmware.bin
